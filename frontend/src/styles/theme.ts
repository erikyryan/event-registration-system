import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1363df",
      dark: "#06283d",
      light: "#47b5ff",
      contrastText: "#dff6ff"
    }
  }
});

export default theme;
